# ===========================================
# 프로덕션 환경 설정 파일 (EC2 배포용)
# ===========================================
# 이 파일을 복사하여 .env로 만들고 실제 값을 입력하세요
# EC2에서: cp .env.production.example .env
# 그 후 nano .env 또는 vi .env로 수정

# Django Settings
SECRET_KEY=your-super-secret-key-minimum-50-characters-long-change-this
DEBUG=False
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,your-ec2-ip

# Database Settings - PostgreSQL (Docker)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=media_platform
DB_USER=postgres
DB_PASSWORD=change-this-to-strong-password
DB_HOST=db
DB_PORT=5432

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# File Upload Settings
MAX_UPLOAD_SIZE=104857600

# Redis Settings (Docker)
REDIS_URL=redis://redis:6379/0

# CORS Settings - 프론트엔드 도메인 추가
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com,https://your-frontend-domain.com

# Email Settings (Optional - Gmail 예시)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# AWS S3 Settings (Optional - 미디어 파일 저장용)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=ap-northeast-2
